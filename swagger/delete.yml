Delete player history of games
---
tags: [Blackjack]
parameters:
  - name: sure
    in: query
    description: Sure must be set to true to delete
    type: boolean
    required: true
  - name: token
    in: path
    description: Token for a single game to delete
    required: false
definitions:
  ErrorMsg:
    type: object
    properties:
      status:
        type: integer
        description: HTTP Status Number
      message:
        type: string
        description: Descriptive Error Message
responses:
  200:
    description: True when delete completes
    schema:
      type: boolean
  400:
    description: Unable to find player by device id
    schema:
      $ref: '#/definitions/ErrorMsg'